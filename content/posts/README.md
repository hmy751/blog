# Posts

실제 발행 중인 글 소스와 앞으로 공개할 글 원고를 둔다. 직접 운영하는 블로그에 올릴 콘텐츠 원천이다.

frontmatter의 `platform` 값은 외부 발행처가 아니라 이 repo의 공개 발행면을 뜻하므로 `Blog`로 둔다.

## Naming

발행 글은 `YYYY-MM-DD-slug.md` 형식으로 둔다. 예:

```text
2026-02-03-blog-1year-retrospective.md
```

파일명 날짜는 frontmatter `date`와 일치해야 한다.
