---
작성일: 2026-07-29
성격: 발행 직전 3중 독립 판별과 main 회수
공개상태: 내부 process
결과: 원고 반영 완료 (커밋 d620f44) / Alex 영상 URL 복구·본문 반영
---

# 발행 직전 3중 독립 판별과 회수

## 목표

공개 전 후보로 승격된 원고를 발행 직전 단계에서 다시 본다. 이미 완료한 사실 정합성 검사를 반복하지 않고, 서로 다른 입력 경계를 가진 독립 판별 3종으로 글의 질감·구조·표현·독자 경험에서 마지막에 끌어올릴 지점을 찾는다. 글의 중심과 후보는 유지하는 전제였고, 판별자가 중심을 다시 열 만큼의 문제를 발견하면 별도 보고하되 자동 채택하지 않기로 했다.

## 입력과 역할

[review-workflow](../../../core/review-workflow.md)의 입력 계약에 따라 세 판별 모두 active-state, process, 이전 원고·review, main의 예상 판정을 받지 않았다. 역할을 새로 구성하며 전역 independent-review 기준(판단 독립성, 원자료 접근, main 가설 비전달)을 점검했다.

- 개선 기준 판별자 ([보고 원문](./06a-improvement-review.md)): 원고와 [직접 원천 묶음](../../../sources/current-origin-and-operation.md)만 읽고 개선 기준을 스스로 세웠다. editorial 기준 문서도 차단해 repo의 판단 프레임에서 독립시켰다. 원천이 연결한 외부 로컬 원본(최초 `current.md` 원형, Alex 전사, raw log, 교차검증, Cofathon 기록)을 직접 열었다.
- 처음 읽는 독자 ([보고 원문](./06b-first-reader-review.md)): 원고 한 파일만 읽었다. 맥락 없는 첫 독서에서 무엇이 이해되고 어디서 막히는지만 관찰했다.
- 장르 거울 critic ([보고 원문](./06c-genre-mirror-review.md)): 원고와 editorial lens 문서(article-types, voice, developer, portfolio-signal, reader-flow, supporting-materials, edit-patterns, page-cadence)만 읽었다.

## 판별 결과

세 판별 모두 중심·구성 유지로 판정했다. 개선 기준 판별자가 원천을 따라가며 마주친 수치(18개 버전, 13회 전환, 20개 자료와 16개 신호, 3-9-3-1, 147→46줄, 82→47줄)는 전부 원고와 일치했고 명백한 사실 충돌은 없었다. 이전 회수에서 사용자가 기각한 방향(`뭔가 계속 유지되고 있다` 체감의 객관화)을 다시 제안한 판별은 없었으며, 셋 다 이 글의 정직성 구조를 최우선 보호 대상으로 꼽았다.

세 판별이 독립적으로 수렴한 발견:

- 제목이 약속한 `지도 위의 커서`를 유일한 구조 도식이 보여 주지 않았다. 실제 최초 committed `current`는 cursor를 가지 위 인라인으로 찍었다.
- 글의 중심 사물인 `current`가 물리적 정체(커밋되는 파일 하나) 없이 등장해 독자가 두 섹션에 걸쳐 역산해야 했다.
- 거의 같은 문장 두 쌍(`정해져 있지 않았습니다` 연속, 본문 bold와 결말의 `모든 대화` 재사용)이 반복으로 읽혔다.

## 채택과 반영

커밋 d620f44 (+30/−28)로 반영했다.

- 수렴 3: 첫 도식에 cursor 인라인 표기, `current` 첫 등장에 `작업 저장소에 함께 커밋하는 파일` 정체 문장, 쌍둥이 문장 두 쌍 해소(결말 보호 문장은 유지하고 본문 쪽만 조정).
- 교차 항목: `그날 밤`에 `구현과 검증까지 통과한 날 밤` 앵커와 `제출 문항 답변` 용어 통일, `계약` 첫 등장을 `지금 구현이 따라야 할 조건을 적은 문서`로 풀고 목록의 `소유합니다`를 일반 동사로, 설계 대목에 가지 보존 규칙과 시작 규칙 예고 추가, 역할별 진입 규칙 단락을 기제가 보이게 펼침(`이전 문서를 건네지 않는 것만으로는 가림이 되지 않았습니다`), 한계 섹션의 `자동 효력` 우산 문장을 섹션 끝으로 옮겨 세 갈래를 닫게 확장, `문서 단위 대신 신호 단위`와 `객관성이라는 외부 판단 기준`으로 실물 부여, frontmatter에서 `product-architecture` 삭제, 숫자 표기 통일.
- 선택지 3 (사용자 승인, 보수적 강도): 부정-재규정 문형 3곳 축소(강한 곳은 유지), 실제 raw log를 회사명 없이 축약 재현한 복기 인용 블록 추가, 두 번째 도식은 형태를 유지하고 화살표 정렬·기호 표기·긴 줄만 정리한 뒤 `다시 열 조건`을 본문 문장으로 회수.
- 추가 1 (제시 목록 밖, 사용자에게 별도 표시): 결말 직전에 `압축 요약만으로 이어진 재진입` 관측 문장. 재진입 교차검증 원천에서 직접 확인했고 주장 상한을 낮추는 방향이다.

새로 넣은 문장의 사실 근거는 반영 전에 원천으로 확인했다: 신호 단위는 signal map의 `문서 수가 아니라 신호 단위` 정의, 객관성은 회사 가중치·객관성 재정리 기록의 `제3자 기준의 중립성 요구를 앞세운 과잉 교정`, 복기 재현은 주 작업 raw log의 실제 복기 event, 압축 요약 관측은 교차검증의 `외부 재독해 없음` 행.

## 조정·기각

- 장르 거울의 결말 직서화 제안은 보호 합의(도입 3중 실패와 결말 3중 호응, `모든 대화가 아니었습니다`)와 충돌해 방향을 조정했다. 결말은 그대로 두고 본문 bold의 `모든 대화를 기억해서가 아니라`를 삭제해 중복만 풀었다.
- 한계 섹션 `###` 삽입안은 기각하고 우산 문장 이동을 택했다. 이 글에 하위 소제목이 없는 형식을 유지하기 위해서다.
- 두 번째 도식의 번호 리스트 교체안은 기각했다. 단계별 trace는 직전 재구성의 의도적 선택이고, 첫 독자 판별에서도 순수익으로 관찰됐다.
- `상태 관리` 태그는 FE 상태 관리 오독을 피해 `세션 연속성`으로 교체했다(main 재량, 사용자 통보). 본문 `cursor` 영문 표기는 이전 회수의 의도적 선택이라 유지했다.

작업 순서에서 한 가지 어긋남이 있었다. 사용자는 수렴·교차를 먼저 반영해 변화를 본 뒤 선택지 3개를 추가하길 원했는데, main이 한 커밋에 모두 반영했다. 사후에 선택지 3개의 변경 위치를 분리해 보고했고 사용자가 수용했다.

## Alex 영상 URL 복구

2026-07-29 앞선 검색은 실패했으나, 같은 날 따옴표로 묶은 재검색에서 해당 라이브 영상을 재발견했다: [5000:60 경쟁이라는 AX 인재전쟁, 2시간만에 끝내기](https://www.youtube.com/watch?v=-pIg6RtF--0). oembed metadata로 커리어해커 알렉스 채널임을 확인했고, 영상을 직접 본 사용자가 해당 영상이 맞다고 확정했다. 본문 링크는 Alex 이름을 채널로, `영상`을 해당 URL로 나눠 연결했다. 인용·사실 판정의 권위는 계속 raw 전사본에 둔다.

## 발행

사용자가 같은 날 발행을 결정해 `content/posts/2026-07-29-current-active-state-operation.md`로 승격했다.

제목과 tags는 발행 직전에 기존 발행글 17편의 실제 패턴과 대조해 다시 봤다. 그 대조에서 두 가지를 정정했다. 유형 라벨이 태그로 나가는 것을 하네스 잔재로 봤으나 `learning-experiment`가 이미 다섯 편에서 같은 방식으로 쓰이고 있었고(다만 사례 연구 글은 유형 태그를 달지 않아 `product-architecture` 제거 자체는 유지), 대안으로 검토한 `AI 하네스`는 본문에 없는 내부 어휘라 철회했다.

- 제목: 은유형 본제목을 유지하고 Noline 글의 `본제목 — 설명 꼬리` 선례에 따라 `— 전체 지도와 현재 위치로 다시 맞추기`를 붙였다. 본제목만으로는 기술 명사가 없어 목록·검색에서 영역이 드러나지 않는다는 관찰을, 어조를 깎지 않는 방식으로 메웠다. 꼬리의 동사는 본문 마지막의 `다시 맞출 자리는 남깁니다`에서 가져와 글과 같은 말로 맞췄다. 사이트 계약상 본문 첫 `#`이 frontmatter `title`과 같아야 중복 렌더링되지 않으므로 H1도 맞췄다.
- tags: 발행글의 `넓은 도메인 + 구체 명사` 조합을 따라 `AI Native`, `Context Engineering`, `Scaffolding`, `세션 연속성`, `개발 워크플로`, `해커톤`으로 정했다. 사용자가 `AI 협업` 대신 `AI Native`를 골랐고, main이 유일한 구체 개념 명사로 제안한 `Scaffolding`과 배경 태그 `해커톤`을 함께 채택했다.

deterministic prepublish script는 통과했다. 승격 뒤 발행 직전 evidence 확인을 다시 돌려 이번 cycle에 새로 들어간 문장(복기 인용, 신호 단위, 객관성, 다시 열 조건, 압축 요약 관측, 시간 앵커, 가지 보존·시작 규칙)과 제목·tags·공개 링크를 원천과 대조했다.

## Evidence 확인이 잡은 사실 오류 두 건

이 확인에서 발행을 막을 충돌 두 건이 나왔고, 둘 다 **이번 cycle의 수정이 만들거나 날카롭게 만든 것**이었다. main이 원천으로 직접 재확인한 뒤 고쳤다.

**과교정과 신호 맵의 선후가 뒤집혀 있었다.** 원고는 `20개 자료 → 16개 신호 → 과교정 → 복귀` 순으로 적었지만, 실제 순서는 `자료 20개 → 과교정 → 복귀 → 신호 단위 재배열(16개 신호)`이다. 근거는 세 가지다. 자기 감사 기록의 `다음 한 동작`이 복구 뒤 시점에 "새 검색을 시작하지 않고 20개 자료를 신호 단위로 재배열한다"라고 적었고, 같은 문서가 복구할 대상으로 "회사 자료가 증명하는 범위와 사용자 문제를 증명하는 범위를 분리해 같은 신호에서 함께 보는 안"을 지목했으며, commit 시각도 넓은 조사(18:14) → 판단 과정 기록(18:39) → 신호 맵(18:55) 순이다. 즉 16개 신호는 과교정 이전 산출물이 아니라 복구의 결과물이다.

이번에 `사용자 문제와 회사 방향을 나눠 적은 신호가 16개 나왔습니다`를 추가하면서 문제가 더 커졌다. 그 분리 기록이 곧 복구된 설계인데, 원고에서는 그것이 과교정보다 앞에 놓였다. 본문을 세 단락으로 다시 나눠 실제 순서로 배치하고, `두 범위를 나눠 적는 일 자체가 과교정에서 되찾은 설계였습니다`를 회수 문장으로 붙였다. 흐름도에도 `신호 단위로 재배열` 단계를 넣어 6단계로 만들었다.

**흐름도의 과교정 문구가 원천의 판정을 뒤집어 읽게 했다.** `회사 방향 vs 사용자 문제로 잘못 분리`라고 적었지만, 원천에서 분리는 복구해야 할 정답이었고 오류는 양자택일로 바꾼 것이었다. 본문 산문은 정확했고 흐름도만 어긋나 있었다. `회사 방향과 사용자 문제를 양자택일로 바꿈`으로 고쳤다.

**시간 앵커가 사실과 어긋났다.** 개선 기준 판별자의 S6 제안을 받아 `그날 밤`을 `구현과 검증까지 통과한 날 밤`으로 바꿨는데, 다섯 문항 작성 시점의 자기 기록은 "Loop 01 교정 세션이 진행 중", "다른 세션이 계약·엔진·README·설치 E2E를 교정 중", "설치된 자연어 E2E는 아직 완료 사실로 쓰지 않는다"라고 적고 있다. 통과 전이었다. `구현 교정과 검증이 아직 진행 중이던 밤`으로 고치고, 다음 문장의 `직전에 끝낸 구현과 검증`도 완료 단정을 뺀 `직전까지 붙들고 있던`으로 바꿨다. 결과적으로 최근 맥락 편향이 왜 그렇게 강했는지가 오히려 선명해졌다.

여기서 남길 교훈은 판별자의 제안이 문장 품질 근거로는 옳아도 사실 근거까지 보증하지는 않는다는 점이다. S6은 독자가 시간을 닫을 앵커가 필요하다는 관찰로는 정확했지만, 그 앵커에 넣을 사실은 main이 원천에서 따로 확인했어야 했다. 다음 cycle에서는 판별 제안 중 **새 사실 주장을 만드는 항목**을 반영 전에 표시하고 원천 확인을 붙인다.

## 발행 뒤 사용자 교정

Evidence 확인은 첫 부제 `전체 지도와 현재 위치 하나로 세션 잇기`가 제목만 노출되는 면(RSS, 카드, 검색 결과)에서 `current` 단독 인과로 읽힐 여지가 있다고 지적했다. 사실 충돌이 아니라 표현 판단이라 고치지 않고 사용자에게 보고했고, 사용자가 `현재 위치` 쪽으로 다시 잡으라고 정해 `전체 지도와 현재 위치로 다시 맞추기`가 됐다. 본문이 이미 쓰는 동사를 꼬리로 올린 셈이라 단독 인과 어감도 함께 빠졌다.

같은 교정에서 사용자는 시간 앵커의 톤도 지적했다. `구현 교정과 검증이 아직 진행 중이던 밤`은 사실은 맞지만 문장이 무겁다고 보아 `구현 교정과 검증이 아직 진행 중인 상태에서`로 바꿨다. 사실 교정이 문장 리듬을 해치면 사실을 되돌리는 대신 표현을 다시 고른다.
