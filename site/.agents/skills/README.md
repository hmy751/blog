# Site Skill Bridge

사이트 개발 전용 Codex skill bridge 자리다.

아직 bridge는 없다. Claude 원천 skill이 생기면 `site/.claude/skills/{name}`을 원천으로 두고, 이 폴더에는 Codex가 읽을 수 있는 bridge만 둔다.

root `.agents/skills/blog-write`는 writing skill bridge이므로 사이트 구현용으로 확장하지 않는다.
